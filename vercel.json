{
    "version": 2,
    "builds": [
        {
            "src": "api/server.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        { "src": "/ws", "dest": "/api/server.js" }
    ]
}
